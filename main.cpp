#include <iostream>
#include "binarytree.h"

using namespace std;

int main()
{
    binarytree tree;
    tree.root=tree.insert(tree.root,value:50);
    tree.root=tree.insert(tree.root,value:50);
    tree.root=tree.insert(tree.root,value:50);
    return 0;
}
